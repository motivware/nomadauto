
<%= form_for([@task.project, @task, @task.comments.build]) do |f| %>
<div class="pt-8 space-y-6 sm:pt-10 sm:space-y-5">
  <div class="space-y-6 sm:space-y-5">
  	<div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start sm:border-t sm:border-gray-200 sm:pt-5">
      <%= f.label :assigned,  class: 'block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2' %>
      <% options_from_collection_for_select(current_account.user_users, :name, :name) %>
      <%= f.collection_select(:owner, current_account.user_users, :name, :name) %>
    </div>
		<div class="sm:grid-cols-3 sm:gap-4 sm:items-start sm:border-t sm:border-gray-200 sm:pt-5">
      <%= f.label :body, class: 'block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2' %><br>
      <%= f.rich_text_area  :body,  class: 'shadow-sm focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm border-gray-300 rounded-md' %>
    </div>
    <br/>
    <div class="pt-5">
        <%= f.submit "Save", class: 'cursor-pointer	inline-flex items-center justify-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto' %>
    </div>
   </div>
</div>
<% end %>
   
