<%= form_for([@project, @profile]) do |f| %>
  <div class="form-group">
    <%= f.label :avatar %>
    <%= f.file_field :avatar, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit "Update Autoshop Profile", class: 'btn btn-primary' %>
  </div>
<% end %>
