<%= render 'users/sidebar' %></br>

<div class="container-fluid">
  <div class="jumbotron">
    <div class="row">
      <div class="col-md-14">
      <%= link_to "Upload Logo", new_project_profile_path(), class: 'btn btn-default' %>
      <br/><br/>
      <% if Profile.exists? %>
      <h3><%= image_tag(@profile.avatar.url(:thumb)) %></h3>
      <% end %>
    </div>
  </div>
</div>
